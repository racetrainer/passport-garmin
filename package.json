{
  "name": "@racetrainer/passport-oauth2-garmin",
  "version": "1.0.0",
  "description": "Passport OAuth2 Strategy implementation for Garmin Connect",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "passport",
    "garmin",
    "oauth2"
  ],
  "author": "Dylan Fiedler (@iamdtfiedler)",
  "license": "MIT",
  "dependencies": {
    "passport-oauth2": "^1.8.0"
  },
  "devDependencies": {
    "@types/passport-oauth2": "^1.8.0",
    "typescript": "^5.0.0"
  }
}
